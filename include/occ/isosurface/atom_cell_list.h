#pragma once
#include <occ/core/atom_cell_list.h>

namespace occ::isosurface {

// Re-export so existing isosurface code can keep using the unqualified name.
using AtomCellList = occ::core::AtomCellList;

} // namespace occ::isosurface
